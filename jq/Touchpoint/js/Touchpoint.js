(function () {
    if (!window.jQuery) { alert("This App requires jQuery"); return; }
    var $ = window.jQuery;


    var example;
    // global variables
    window.Touchpoint = {
        example: example,
        example2: example
    };
})();